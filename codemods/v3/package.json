{
	"name": "msw-3-upgrade-recipe",
	"version": "0.1.0",
	"description": "Migrate your project from MSW v2 to v3",
	"type": "module",
	"devDependencies": {
		"@codemod.com/jssg-types": "^1.6.3",
		"typescript": "^7.0.2"
	},
	"scripts": {
		"test": "for script in scripts/*.ts; do npx codemod@latest jssg test -l tsx \"$script\" \"tests/$(basename \"$script\" .ts)\" --disable-analytics || exit 1; done",
		"check-types": "npx tsc --noEmit"
	}
}
